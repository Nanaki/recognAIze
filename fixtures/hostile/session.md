Notes libres sans structure de tour reconnue. Pas de **Locuteur** isolé sur sa
propre ligne ici, ni de `### Tour N` — juste du texte brut, pour vérifier que
`session.ts` ne lève jamais sur une structure non reconnue et produit un
digest vide avec un avertissement `no_recognized_turns`.
